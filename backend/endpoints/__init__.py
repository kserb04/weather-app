# from fastapi import APIRouter
# from .weather import router as weather_router
# from .info import router as info_router

# router = APIRouter()
# router.add_route(weather_router, prefix="/weather")
# router.add_route(info_router, prefix="/info")